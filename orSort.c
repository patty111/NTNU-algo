#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <stdint.h>
#include <string.h>

typedef struct{
    int x;
    int y;
}vertice;

int cmpfunc(const void* a, const void* b){
    
}

int main(){
    int n = 6;
    vertice* V = (vertice*)malloc(sizeof(vertice) * n);











    return 0;
}